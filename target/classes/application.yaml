spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/heartbeat_db}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          time_zone: Asia/Ho_Chi_Minh
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB
      file-size-threshold: 2KB

jwt:
  signerKey: ${JWT_SIGNER_KEY}
  valid-duration: 1800
  refreshable-duration: 604800

# Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:your_cloud_name}
  api-key: ${CLOUDINARY_API_KEY:your_api_key}
  api-secret: ${CLOUDINARY_API_SECRET:your_api_secret}

google:
  client-id: 347228409229-7gids663gu70dsrhtet40u9krov3c941.apps.googleusercontent.com

sendgrid:
  api-key: ${SENDGRID_API_KEY}
  from-email: ${SENDGRID_FROM_EMAIL}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# MoMo Payment Configuration
momo:
  partner-code: ${MOMO_PARTNER_CODE:MOMOPC9B20251018_TEST}
  access-key: ${MOMO_ACCESS_KEY:AdPVP3a4q5326kp4}
  secret-key: ${MOMO_SECRET_KEY:hlcJSeEbHocX1Hi8XZo7ZsVh4HljxnFI}
  endpoint: ${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api/create}
  redirect-url: ${MOMO_REDIRECT_URL:http://localhost:5173/payment/result}
  ipn-url: ${MOMO_IPN_URL:http://localhost:8080/api/payment/momo/ipn}
  request-type: ${MOMO_REQUEST_TYPE:captureWallet}

